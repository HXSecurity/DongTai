apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dongtai.fullname" . }}-test-connection"
  labels:
    {{- include "dongtai.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dongtai.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
